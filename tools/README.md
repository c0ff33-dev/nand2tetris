The original nand2tetris tooling is written in Java and requires Java Runtime Environment (JRE) to run, the latest available version is recommended / no specific target version is specified.

Install JRE on WSL:
sudo apt update
sudo apt install default-jre

Install JRE on Windows:
https://www.java.com/en/download/